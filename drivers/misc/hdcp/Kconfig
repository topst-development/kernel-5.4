#
# HDCP Configuration
#

config SNPS_HDCP_DP
	bool "HDCP for Synopsys DesignWare Cores DisplayPort"
	depends on TCC_DP_DRIVER_V1_4
	default y
	help
	  Say Y here if you want to support the /dev/hdcp device.
	  The /dev/hdcp device is used to control Synopsys HDCP IP.
	  It supports HDCP1.3.
	  When in doubt, say "Y".

